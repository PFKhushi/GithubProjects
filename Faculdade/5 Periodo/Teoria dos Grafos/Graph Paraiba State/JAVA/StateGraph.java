import java.util.*;

class StateGraph {

    public class Cidade {
        String nome;
        int ID;
        Vector<Integer> conexoes; // Recebe os IDs das cidades com as quais tem conexao
    }

//////////////////////////////////////////////////////

    public static void main (String[] args) {

        Vector<Integer> matrix = new Vector<>(); // É um vetor 2D que segue a formula [coluna * num_elementos + linha]


    }
//////////////////////////////////////////////////////
    public static Vector<Integer> fazMatrix(ArrayList<Cidade> cidades){

        Vector<Integer> nova_matrix = new Vector<>();

        return nova_matrix;
    }
}
